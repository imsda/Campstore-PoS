require('./env').loadEnv(); const { fileDateStamp, formatTime, now } = require('../datetime'); const fs=require('fs'), path=require('path'); const src=process.env.DATABASE_PATH||'./data/campstore.sqlite'; fs.mkdirSync('backups',{recursive:true}); const dest=`backups/campstore-${fileDateStamp()}-${formatTime(now()).replace(/[: ]/g,'-')}.sqlite`; fs.copyFileSync(src,dest); console.log(dest);
